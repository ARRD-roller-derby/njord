import { useContext } from "react";
import {
  EventAttendeesResult,
  EventAttendeesProps,
} from "./event-attendees.type";

//TODO finish
export const useEventAttendees = ({ event }: EventAttendeesProps) => {
  return {};
};
